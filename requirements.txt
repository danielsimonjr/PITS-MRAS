# Phase-1 runtime dependencies (what the Phase-1 code imports).
# Dev tooling lives in requirements-dev.txt; logging extras (tensorboard,
# wandb) live in setup.py extras_require["logging"].
numpy>=1.24.0
scipy>=1.10.0
torch>=2.0.0
pyyaml>=6.0.0
