; flake8 has no native pyproject.toml support, so its config lives here.
; All other tool config (black, isort, mypy, pytest) is in pyproject.toml.
[flake8]
max-line-length = 100
extend-ignore = E203, W503
exclude = .git,__pycache__,build,dist,*.egg-info
