# Development toolchain for PITS-MRAS.
# Runtime dependencies live in requirements.txt; these are dev/CI only.
# Ruff replaces flake8 + black + isort (lint, format, and import-sorting).
# PINNED EXACTLY: the formatter's output changes between ruff releases, so an
# unpinned range makes `ruff format --check` pass locally but fail in CI when CI
# floats to a newer ruff. Bump this pin deliberately (and reformat) when desired.
ruff==0.8.1
mypy>=1.4.0
pytest>=7.4.0
pytest-cov>=4.1.0
# jaxtyping is dev-only: shape annotations are imported under TYPE_CHECKING,
# so it is never required at runtime (only for type-checking / IDEs).
jaxtyping>=0.2.0
